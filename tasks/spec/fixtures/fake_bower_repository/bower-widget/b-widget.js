define( [], function() {
   'use strict';

   function Controller( context ) {
      /* :) */
   }

   ///////////////////////////////////////////////////////////////////////////////////////////////////////////

   return {
      create: Controller,
      injections:  [ 'axContext' ],
      name: 'bWidget'
   };

} );
