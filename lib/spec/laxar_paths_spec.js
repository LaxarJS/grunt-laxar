/**
 * Copyright 2014 aixigo AG
 * Released under the MIT license.
 * http://laxarjs.org/license
 */
var laxar_paths = require( '../laxar_paths' );

describe( 'laxar_paths( config, [options] )', function() {
   'use strict';

   it( 'resolves the laxar path constants with the given require configuration' );

} );

